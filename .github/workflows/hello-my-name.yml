#Workflow name
name: hello to self

#Workflow trigger
on:
  workflow_dispatch: 

#Workflow Jobs
jobs:

  #Job name
  greet:

    #GitHub runner
    runs-on: ubuntu-latest

    steps:
      - name: say hello to self
        run: echo "Hello Sayee"
